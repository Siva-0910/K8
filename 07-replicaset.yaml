apiVersion: apps/V1
kind: ReplicaSet
metadata:
  name: demo-ReplicaSet
  labels:
    purpose: demo-rs 
    project: roboshop
    env: dev
spec:
  replicas: 2
  selector:
    name: rs
    project: roboshop
    env: dev

template:
  metadata:
    labels:
    purpose: demo-rs 
    project: roboshop
    env: dev
spec:
  - containers:
    name: nginx
    image: nginx        