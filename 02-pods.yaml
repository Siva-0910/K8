apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: roboshop
    environment: test
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "68mi"
        cpu: "100m"
      limits:
        memory: "120mi"
        cpu: "150m"